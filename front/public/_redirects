# ⚠️ SPA 폴백. 이 파일이 없으면 딥링크가 전부 404 다 (#112).
#
#   라우터가 BrowserRouter 라 /trips/101/seats 같은 경로는 **파일이 아니다** —
#   dist 에는 index.html 하나뿐이고, 그 경로는 JS 가 읽는 문자열이다.
#   링크 클릭은 서버를 안 거치니 멀쩡하지만, **새로고침 · 북마크 · 공유 링크**는
#   브라우저가 그 경로를 서버에 직접 묻는다. Pages 는 파일을 못 찾고 404 를 준다.
#
#   ⚠️ 301/302 가 아니라 **200** 이어야 한다. 리다이렉트면 주소창이 /index.html 로
#   바뀌어 **라우터가 읽을 URL 자체가 사라진다** — 404 대신 항상 홈이 뜬다.
#
#   실제 파일이 먼저 매칭되므로 /assets/*.js 는 그대로 나간다.
#
#   ⚠️ vite dev 서버는 이걸 기본으로 해준다. 그래서 **배포해야 드러난다.**
/*    /index.html   200

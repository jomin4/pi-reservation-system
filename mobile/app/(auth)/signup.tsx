import { Placeholder } from '@/ui/Placeholder'

/** `M-08` 회원가입 — 이메일 인증 단계가 없다 (범위 밖). 폼은 #47 이 채운다 */
export default function Signup() {
  return <Placeholder id="M-08" title="회원가입" note="이메일 · 비밀번호 · 이름 · 연락처 (#47)" />
}
